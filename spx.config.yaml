validation:
  paths:
    exclude:
      - .work/
      - spx/46-claude.outcome/
  literal:
    values:
      include:
        - --idempotency-key <key>
        - --payload <payload-source>
        - append-finding
        - append-scope
        - event
        - finding
        - journal
        - node_modules
        - scope
        - verification
        - verify
