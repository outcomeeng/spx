# Plan: Diagnose

## Harness vocabulary alignment

`spx/12-agent-harness.pdr.md` includes top-level command domains that inspect configured-agent sessions, plugin installation, marketplace configuration, worktree occupancy, and local harness health. Align this node's specs, diagnostic provider names, manifest vocabulary, command text, and report text so each check identifies whether it describes the harness, a configured agent, an agent adapter, or an agent session.
