# Plan: Validation

## Harness governance (complete)

Govern the still-ungoverned validation test harnesses and generators per the **Remaining harness governance program** in `spx/PLAN.md` (uniform approach, audit gates, and literal-collision lessons).

**Outcome: one node.** Surveying each module's consumer usage before authoring (the rule the survey-first lesson establishes) showed the validation harnesses are heavily exercised by their many domain test consumers, so almost every module is statement-fully-covered and gets no node. Only `eslint.ts` carried reusable surface no consumer reached (the parser-free tester, the explicit-tester runner, `validationBuiltinRule`'s throw path, `severityOf`'s `undefined` branch) and earned a governing node. Two dead exports the survey turned up were removed (`validationAstRuleTesterLanguageOptions`, `MARKDOWN_TEST_SLICE`).

**Governance model — builders, not drivers (refined while governing `eslint.ts`).** Govern only the reusable-builder surfaces — factories, recording objects, case-run constructors, env harnesses, generator factories — each with a `[test]` that adds net-new statement coverage of the harness and `[audit]` discipline compliance. A **scenario-driver** harness — one whose single export runs a domain node's own scenarios and embeds their assertions (`runMarkdownValidationScenario`, `runValidationLintPolicyScenario`, `runValidationPipelineScenario`) — gets **no** governance node: its behaviour is the domain node's behaviour, already governed by that node's own assertions, so a governance `[test]` would be vacuous (zero net-new coverage). A module may also split by evidence level: `eslint.ts`'s `RuleTester` builders are governed l1, while its lint-text helpers (`createValidationEslint`, `lintValidationText`, `messagesForRule`) are l2 and stay exercised by the existing `32-ast-enforcement.enabler/tests/eslint-rules.scenario.l2.test.ts` consumer. And a **fully consumer-covered** generator gets **no** node either: when every live builder is already exercised — its cross-cutting config by a harness node, its case factories by the owning domain's own rule tests — the coverage gate has nothing net-new to govern, so a node would carry only a vacuous `[test]`. `testing/generators/validation/ast-enforcement.ts` is that case (its sole uncovered export, `validationAstRuleTesterLanguageOptions`, was dead and is removed).

Module disposition (builder → governing node; driver → owning domain node):

- `testing/harnesses/validation/eslint.ts` (rule-tester builders) → **done**: `spx/41-validation.enabler/32-typescript-validation.enabler/32-ast-enforcement.enabler/43-eslint-test-harness.enabler` (governs the `RuleTester` factories, runners, builtin resolution, and `severityOf`).
- `testing/generators/validation/ast-enforcement.ts` (rule-tester config + case-run factories) → **no node** (fully consumer-covered): its `validationEslintRuleTesterLanguageOptions` and `validationRuleTesterHooks` are covered by the `43-eslint-test-harness.enabler` test, and its case factories by `32-ast-enforcement.enabler`'s own rule tests. The `32-ast-enforcement` sub-enabler's test-support governance is therefore complete.
- `testing/harnesses/validation/cli.ts` (builders) → **no node** (fully covered): every export is exercised by its domain consumers — `runValidationSubprocess` across 5 domain test files, `expectValidationSubprocessResult` across 4, the rest by 1–2, and `validationCliPackagedArgs` through the internal `runValidationSubprocess` path.
- `testing/harnesses/validation/lint-policy.ts` (driver `runValidationLintPolicyScenario`) → no node; governed by `spx/41-validation.enabler/32-typescript-validation.enabler/32-lint.enabler`'s own tests.
- `testing/harnesses/validation/markdown.ts` (driver `runMarkdownValidationScenario`) → no node; governed by `spx/41-validation.enabler/65-markdown-validation.enabler`'s own tests.
- `testing/harnesses/validation/pipeline.ts` → **no node**: `runValidationPipelineScenario` is a driver (validation domain tests), and `expectValidationStructuralMapping` is covered by its 2 consumers.
- `testing/harnesses/validation/subprocess.ts` → **no node here** (covered by 3 consumers across `13-cli` and validation); its process-lifecycle concern belongs to the CLI batch (`spx/13-cli.enabler`) — reconcile there, do not duplicate.
- `testing/harnesses/with-validation-env.ts` (`withValidationEnv`) → **no node** (fully covered): `withValidationEnv` has 5 domain test consumers (plus 2 harness-side importers, `pipeline.ts` and the `validation.ts` generator).
- `testing/generators/validation/{lint-policy,markdown,validation}.ts` → **no node** (fully covered): their factories are exercised by the validation domain tests, and `validation.ts`'s per-arbitrary exports are bundled and consumed through the bundle. The dead `MARKDOWN_TEST_SLICE` is removed.

The validation batch is complete: one governing node (`43-eslint-test-harness.enabler`), every other module fully consumer-covered or a driver, and two dead exports removed.
