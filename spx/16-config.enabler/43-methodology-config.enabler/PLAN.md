# Plan: methodology config

## Pending steps

1. Route the untagged assertions through `/apply` and `/verify`; the three test files under `tests/` predate the sentinel's removal and import symbols the descriptor no longer exports, so they are re-derived from the current assertions rather than patched.
