# Plan: Journal surface node (transitional catalyst misplacement)

> **Reconcile against `spx/PLAN.md` first.** This node is the catalyst misplacement the corrected model exists to fix. The `spx journal` node was moved here **wholesale**, carrying its persistence substrate — the `open` / `append` / `read` / `seal` / `render` event-store logic in `src/domains/journal/` (run-state, run-scope, backend selection) and the five-verb `11-journal-channel.adr.md` invariant — into the CLI surface layer, which `spx/60-surfaces.enabler/surfaces.md` forbids (a surface node never owns product-library semantics). Per additive migration (root `spx/PLAN.md` delivery-order step 4), that substrate splits back to a future persistence / verification node and only the thin `spx journal` surface binding stays here; the `list` / `read-set` verb question is open (root plan open questions). The co-located spec `journal.md` still declares the seven-verb `spx journal` command as it stands today — that is the current state, not the target. Where this note predates that model, the root plan governs.
