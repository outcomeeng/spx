import { describe, it } from "vitest";

describe("compact directive byte conformance", () => {
  it.todo(
    "emits compact-source stdout equal to the exact bytes of the installed methodology package's manifest-named compact-recovery resource",
  );
});
