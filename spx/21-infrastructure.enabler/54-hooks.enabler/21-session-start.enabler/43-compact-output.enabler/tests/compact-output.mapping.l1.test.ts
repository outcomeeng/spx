import { describe, it } from "vitest";

describe("compact directive resolution failure mapping", () => {
  it.todo(
    "maps every unresolved-directive condition to no compact-source stdout, a stderr diagnostic naming the failed resolution step, and successful hook completion",
  );
});
