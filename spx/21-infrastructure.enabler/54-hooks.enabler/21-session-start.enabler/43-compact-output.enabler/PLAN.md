# Plan: compact output

## Pending steps

1. Route directive resolution in `src/lib/methodology/compact-recovery.ts` and `src/interfaces/hooks/session-start.ts` through the shipped-tree reader rooted at spx's package root — the reader `spx/31-spec-domain.enabler/43-context-ingestion.enabler/65-understand-payload.enabler` uses — addressed by the payload product's declared methodology line and the invoking coding agent; no product-directory or package-location resolution remains.
2. Re-fixture the compact-output tests, `testing/harnesses/hooks/compact-recovery.ts`, and `testing/generators/config/descriptors.ts` on a temp tree root shaped as `methodology/{line}/{agent}/spec-tree/skills/understand/`.
