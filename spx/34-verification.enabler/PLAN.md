# Plan: verification domain

> Reconcile against `spx/PLAN.md`, `spx/spx.product.md`, `spx/34-verification.enabler/verification.md`, and the affected child specs and decisions first. This note is coordination, not product truth. The methodology model it implements lives in the external Outcome Engineering reference (`reference/spec-tree-reference.md`): verification is the five types, and the node status claim supersedes the exclusion list.

## Program: test verification recording and claim

This is the authoritative, all-slice plan for making `test` a first-class verification type whose execution is recorded as a snapshot and folded into the node status claim. The claim-side detail is cross-referenced from `spx/31-spec-domain.enabler/21-node-status.enabler/PLAN.md`, which points back here.

### The corrected model (why this program exists)

- **Verification is the five types** — validate, test, evaluate, audit, review. `test` is a first-class verification type, not a separate track. The current split (deterministic `spx test`/`validation` walled off from the verification-run recording) is a relic; `spx/34-verification.enabler/verification.md` line 21 and `spx/60-surfaces.enabler/21-cli-surface.enabler/13-verify-command-surface.pdr.md` still encode that split and are slated to change.
- **Persistence shape follows the evidence's temporal structure, not the verdict mode.** Two substrates sit over `spx/18-state.enabler/43-record-store.enabler`: the **journal** (`spx/18-state.enabler/71-appendable-journal-store.enabler`) holds an incremental event history — scope and finding events appended as the run discovers them, streamable to a backend before the run completes; the **snapshot store** (`spx/18-state.enabler/71-snapshot-store.enabler`) holds one whole projected payload per run. Which substrate a run uses depends on how its evidence *arrives*, and that is **orthogonal** to whether spx or an agent drives it. A **validation** run is deterministic yet **journal-shaped** — `src/validation/registry.ts` exposes ordered pipeline stages (lint, typecheck, AST, circular, literal, markdown, formatting), each streaming its result and its "problems" (findings) as it completes (`validation.md`: "reports each tool's result as it completes"). **Audit/review** are agentic and journal-shaped. A **vitest** run is deterministic and **snapshot-shaped** — its `json` reporter batches one whole payload at finish. The earlier "deterministic ⇒ snapshot, agentic ⇒ journal" split conflated the two axes; **validation is the counterexample** — deterministic and journal-shaped. Both state nodes are already consumer-agnostic (`snapshot-store.md`: "such as a test verification run's per-case snapshot"; `appendable-journal-store.md`: "and any journal consumer"), so **neither needs amendment** — the merged snapshot-store node stands. Snapshot recording must **not** couple to anything named `journal`/`agent` (`snapshot-store.md` forbids it); journal recording is the home for deterministic streaming types (validation) alongside the agentic ones.
- **Claim vs. verification.** The committed `spx.status.json` is a **claim** authored by whoever pushes; **CI is the arbiter** that reproduces claimed-passing evidence and refutes regressions. `spx spec status --update` **folds** available local evidence into the claim and **executes nothing**. The present behaviour — `--update` re-running per-node tests when recorded evidence is stale/failing/absent (`spx/31-spec-domain.enabler/54-spec-cli-commands.enabler/21-status-testing-delegation.adr.md`) — is the relic that produces the whole-tree serial re-run (observed: one failing test → 60 serial vitest processes, ~7m42s vs a 2m07s batch). It is replaced by fold-only.
- **The spec→test→source graph is the addressing spine** (`spx/25-outcomeeng.enabler/31-spec-tree.enabler/21-graph.enabler`: `21-spec`, `32-test`, `43-source`). The test graph maps which test file is evidence for which node (claim addressing); the source graph derives staleness from spec-linked tests plus provider facts, **not language import graphs alone** (`.../11-graph-library-boundary.adr.md`, `21-graph.enabler/graph.md`). Today discovery/changed-set uses the TypeScript import-closure walker (`src/test/languages/typescript.ts`) — the "garbage" the graph replaces. This program **reuses that discovery as-is and adds nothing to it**; the graph is a later slice.

### Layering (per `spx/14-cli-composition.adr.md`)

Every piece follows library → domain composition → CLI surface, with reusable libraries under `src/lib/`:

| Layer                               | Modules                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| library (`src/lib/`)                | reuse `src/lib/state-store/` (`.spx/` addressing); **new** snapshot-store lib over it; `src/lib/spec-tree/` + `src/lib/node-status/` (claim fold)                                                                                                                                                                                                  |
| domain (`src/domains/`)             | `src/domains/verify/` — the recorder domain, unchanged; snapshot-shaped `test` derives its terminal outcome from the projected snapshot (pure), registering no journal scope/finding validator (see *Execution surface*)                                                                                                                           |
| command (`src/commands/`)           | `src/commands/verify/` — the recorder, **unchanged**; **new** `src/commands/verification-exec/` — the executor: drive the vitest runner, project JSON → snapshot, persist via the snapshot-store lib                                                                                                                                               |
| CLI surface (`src/interfaces/cli/`) | `src/interfaces/cli/verify.ts` keeps owning `spx verification run` Commander parsing (`13-verify-module-structure.adr.md`, **unchanged**); the `spx verification test` executor noun parses through the executor's own boundary; shared-parent composition deferred to `/decompose`+PDR. The snapshot store and the journal are **never** exposed. |

### Slice 1 — atomic snapshot vertical (TypeScript)

**Demonstrable value:** `spx verification test <scope>` (the executor noun — see *Execution surface* below) invokes vitest **once** over the selected TypeScript files, captures the atomic end-of-run JSON, projects it into a **per-case snapshot**, persists it in the snapshot store (scope-addressed by worktree/branch, one immutable artifact per run, multiple per address), and `spx spec status --update` **folds** real per-reference outcomes into `spx.status.json` **executing nothing** — a passing and a failing test file in one node resolve independently.

Vitest evidence: the built-in `json` reporter is **atomic** — `JsonReporter.onTestRunEnd` builds the whole result and `writeReport` does one `writeFile` at completion (`vitest/dist/chunks/index.UpGiHP7g.js`), so the test executor appends its per-case units at `finish` rather than streaming. Per-case streaming exists via `onTestCaseResult`; that is the observability slice. This atomicity is an adapter fact of vitest, not a property of the `test` type or the contract — `spx verification validate` streams its pipeline stages natively.

Node set (`/apply` order; existing = reuse, NEW = compose):

1. **NEW** `spx/18-state.enabler/71-snapshot-store.enabler` — whole-payload snapshot persistence; **multiple immutable snapshots per scope** (record-store-style run artifacts: no clobber, round-to-round comparison, stable home replacing temp-`outputFile` chasing); reuses `spx/18-state.enabler/43-record-store.enabler` run-file mechanics over `spx/18-state.enabler/32-scope-addressing.enabler`; same-index peer of `spx/18-state.enabler/71-appendable-journal-store.enabler`; **NEVER** imports or binds `spx/15-agent-run-journal.enabler` or `spx/18-state.enabler/71-appendable-journal-store.enabler`. Index 71 confirmed by ordering evidence (consumes `43-record-store`; independent peer of `71-appendable-journal-store`).
2. **NEW** `test` **executor** (snapshot-shaped, see *Execution surface*): the `spx verification test` executor in the new `src/commands/verification-exec/` drives vitest, projects the atomic end-of-run JSON into a per-case snapshot, and persists it through the snapshot-store lib. Because vitest-test is **snapshot-shaped**, it registers **no** journal `scope`/`finding` validator — its evidence is the snapshot document, not a journal event history (contrast the journal-shaped `validate` type below, which *does* register scope+finding in the verify recorder's evidence-validator registry per `spx/34-verification.enabler/32-verify.enabler/13-verify-module-structure.adr.md`). Node boundaries and indices settled in `/decompose`.
3. **REUSE/extend** `spx/41-test.enabler/21-typescript-test.enabler` — vitest adapter emits `--reporter=json --outputFile=<path>` and returns the JSON path, not just the exit code; reuse `spx/41-test.enabler/85-agent-test-output.enabler` capture-to-file primitive.
4. **NEW** snapshot projection (vitest JSON → per-case snapshot document) — placement (`spx/41-test.enabler` vs. the new `test` type node) settled in `/decompose` when this slice reaches it.
5. **NEW/extend** `spx/60-surfaces.enabler/21-cli-surface.enabler` — add the `spx verification test` executor noun beside `spx verification run`; amend `13-verify-command-surface.pdr.md` per the table below.
6. **REUSE/extend** `spx/31-spec-domain.enabler/21-node-status.enabler` — additive fold-from-snapshot path in `--update` (beside the existing path); maps test file → node via existing spec-tree evidence references.

**Snapshot domain reservation (binding on the consumer that captures):** the snapshot store addresses `{scope}/{domain}/runs/run-*.jsonl`, and `spx/41-test.enabler/43-last-run-evidence.enabler/11-last-run-file.adr.md` reserves `.spx/worktree/test/runs/` — the `test` domain at the worktree scope — for testing last-run evidence. Enumeration cannot tell a foreign run record from a snapshot, so the test-verification capture takes a domain distinct from `test` (never `STATE_STORE_DOMAIN.TEST`, which `src/test/run-state.ts` already occupies). Reserve that domain when node 2 or node 4 lands, or reconcile the two decision records first.

### Execution surface — the noun after `verification` decides record vs. execute

Verification has five types, and every kind lives under `spx verification`, so `spx test` and `spx validation` retire into it. What separates *recording* from *execution* is the noun after `verification`:

- `spx verification run <verb>` — the **recorder**. Type-agnostic; records and replays a run its **caller** drove. The descriptor already names it "Record and replay a typed verification run," and its verbs (`start`, `input`, `scope add`, `finding add`, `finish`, `status`, `render`) never execute. This is where the **agentic** types — `audit`, `review` — report, because spx never drives an agent.
- `spx verification <type> <verb>` (`spx verification validate`, `spx verification test`, `spx verification eval`) — the **executor**. spx drives the deterministic runner itself and records evidence in the run's shape: streamed scope/finding journal events for journal-shaped runs (validation stages+problems, eval cases), a whole-payload snapshot for snapshot-shaped vitest-test.

An executor noun exists **exactly for the deterministic types** (validate, test, eval) — the ones spx may run directly. The agentic types get no executor noun: `verification.md`'s "spx never drives a verification agent" holds, and vitest/ESLint are runners, not agents. (The exact noun/verb grammar — whether the executor reuses the recorder's lifecycle verbs or exposes a single run verb, and how `--verification-type` on `run` relates to the type noun — is a `/decompose` + PDR question; the separation principle above is what this plan fixes.)

**Layering keeps the recorder untouched.** The executor is a *client* of the recorder in a sibling module:

- `src/interfaces/cli/verify.ts` — the `spx verification run` descriptor, **unchanged**; it stays the only module parsing Commander for the run family, per `13-verify-module-structure.adr.md`.
- the `spx verification <type>` executor nouns register through the executor's own CLI boundary — their Commander parsing is **not** folded into `verify.ts`. How the shared `spx verification` parent composes the run descriptor and the executor descriptor is the `/decompose`+PDR noun-grammar question below.
- `src/commands/verify/` — the recorder, pure run lifecycle over the journal, **unchanged**.
- `src/commands/verification-exec/` — the executor: drives `src/test/registry.ts` / `src/validation/registry.ts` and records in the run's shape — journal-shaped runs feed each unit to the injected recorder port; snapshot-shaped vitest-test projects the whole payload through the snapshot-store lib.

Two `13-verify-module-structure.adr.md` invariants stay literally true, so it needs no amendment: "verifier execution remains outside the verify module" (execution lives in the exec module; the verify module still only records) and "`src/interfaces/cli/verify.ts` is the only verify module that parses Commander" for `spx verification run` (the run descriptor is untouched; the executor parses through its own boundary).

**Evidence shape is per-type; streaming granularity is an adapter property.** A **journal-shaped** run appends one event per progress unit — a validation **stage** and its **problems** (`src/validation/registry.ts` exposes ordered pipeline stages, the `[1/7]…[7/7]` output), an eval **case**, an audit/review **finding**; `verification.md` asserts such a run "streams to its backend incrementally as events append." Streaming *granularity* is the adapter property: validation streams its stages natively, while an adapter that can only emit a terminal payload appends its units at `finish` — degraded observability under the same journal contract. **Vitest-test is snapshot-shaped, not journal-batched:** its `json` reporter yields one whole payload, recorded as a snapshot document (`snapshot-store.md`: "without appending an event history"), so per-case pass/fail is snapshot content the fold reads — not a journal `finding` event. Whether a future streaming test adapter (`onTestCaseResult`) makes test *also* journal-shaped is the observability slice, not slice 1.

**Retirement is an equivalence gate, not a judgment call.** Nothing retires until a per-reference **equivalence test** shows the executor's outcomes match the retiring surface's over a fixture corpus — `spx verification test` vs `spx test`'s `TestRunState`, `spx verification validate` vs `spx validation`. Until then the surfaces coexist. "Fully baked" becomes a passing assertion.

**Decision records this program amends (truth flows down — amend before specs, tests, and code):**

| Decision record                                                                               | Rule it currently carries                                                                                                                               | Disposition under the noun-sibling model                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `spx/60-surfaces.enabler/21-cli-surface.enabler/13-verify-command-surface.pdr.md`             | Property 2: `spx validation`/`spx test` remain the top-level deterministic execution surfaces; NEVER move them under `spx verification run`             | **Amend.** They become sibling nouns under `spx verification` (`spx verification validate` / `test`), not children of `run`. The letter ("not under `run`") survives; the top-level intent inverts — state it plainly.                                                                                                                                                                                                                                                                                                               |
| `spx/34-verification.enabler/verification.md`                                                 | Deterministic `spx test`/`spx validation` stay top-level; the verify surface records **scoped agentic evidence**; spx never drives a verification agent | **Amend** two clauses. (1) The top-level-deterministic clause: deterministic types get an executor noun under `spx verification`. (2) The "scoped **agentic** evidence" clause: the journal substrate carries any **journal-shaped** run's streamed scope/finding events — deterministic `validate` alongside agentic `audit`/`review` — so evidence routing keys on shape, not verdict mode. The "never drives an **agent**" clause **stands**: spx drives deterministic runners (vitest, the validation pipeline), never an agent. |
| `spx/34-verification.enabler/32-verify.enabler/13-verify-module-structure.adr.md`             | "Verifier execution remains outside the verify module"; `src/interfaces/cli/verify.ts` is the only module parsing Commander for `spx verification run`  | **Stands unamended** — the executor design preserves **both** invariants: execution lives in `src/commands/verification-exec/` (the verify module still only records), and `src/interfaces/cli/verify.ts` keeps owning `spx verification run` Commander parsing (the executor nouns parse through their own boundary, not folded into `verify.ts`). The shared-parent composition is a `/decompose`+PDR question that must hold both invariants.                                                                                     |
| `spx/31-spec-domain.enabler/54-spec-cli-commands.enabler/21-status-testing-delegation.adr.md` | `--update` runs the per-node run when recorded evidence is stale, failing, or absent                                                                    | **Amend** (independent of the above, required regardless): `--update` folds recorded evidence and executes nothing; absent evidence leaves a reference's claim unchanged.                                                                                                                                                                                                                                                                                                                                                            |
| `spx/31-spec-domain.enabler/21-node-status.enabler/15-status-file-contract.pdr.md`            | The read-versus-refresh split the delegation ADR rests on                                                                                               | **Amend** to the fold-only contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `spx/41-test.enabler/43-last-run-evidence.enabler/11-last-run-file.adr.md`                    | Reserves `.spx/worktree/test/runs/` for `TestRunState`                                                                                                  | **Stands unamended** until a later slice reconciles the two stores; the capture takes a distinct domain.                                                                                                                                                                                                                                                                                                                                                                                                                             |

Code follows spec: the recorder's `VERIFY_VERIFICATION_TYPE` (`{audit, review}`, in `src/domains/verify/verify.ts` — the vocabulary whose finding payloads `spx verification run finding add` validates) gains the **journal-shaped** deterministic types `validate` and `eval`, which register scope/finding validators, once the specs admit them. Snapshot-shaped `test` does **not** join it — it never enters the recorder's `finding add` lifecycle, so the executor carries its own type tag in the `src/commands/verification-exec/` snapshot path, not the recorder registry.

**Finding-validator resolution (settled — evidence shape decides, not verdict mode).** A **journal-shaped** type registers the full evidence surface — `scope` + `finding` + terminal: `validate` (stage = scope, problem = finding) joins the agentic `audit`/`review` in the verify recorder's evidence-validator registry. A **snapshot-shaped** `test` run over the vitest `json` adapter records a whole-payload snapshot, appends no `scope`/`finding` journal events, and therefore registers neither — its per-case pass/fail lives inside the snapshot document that `--update` folds. Consequently `spx/34-verification.enabler/32-verify.enabler/ISSUES.md`'s next-action gate does **not** fire in slice 1: journal-shaped types register the full surface, so `status`/`render` advertise only actions the API accepts; snapshot-shaped `test` is not a journal run whose next-actions the gate governs. The gate stays latent only for a hypothetical journal-shaped type that registers *part* of scope/finding — none of the five do — so it is out of slice-1 scope.

Reused untouched: the journal backend, existing discovery / `spx/41-test.enabler/95-changed-set-planning.enabler` (add nothing).

Boundaries — **in:** TypeScript only; one vitest invocation batched at `finish`; per-case snapshot; the snapshot-store node; the `test` executor noun; `--update` fold; `spx test` and `spx verification test` **coexist**. **out:** stage-streaming observability (an adapter capability, deferred, not forbidden by the contract); the `spx verification validate`/`eval` executors; the graph; `EXCLUDE` removal; `spx test`/`spx validation` retirement (gated on the equivalence test); `TestRunState` reconciliation; Python; any journal/agent coupling.

Route: `/decompose spx/18-state.enabler` (snapshot-store node — **landed**) → `/author` the decision-record amendments in the table above (surface separation + fold-only) → `/decompose spx/60-surfaces.enabler/21-cli-surface.enabler` and `spx/34-verification.enabler` for the executor/recorder node boundaries → resolve the `finding`-validator gate → `/author` specs → `/apply` per node in index order → `/merge`.

### Slice 2 — authored lifecycle status; retire `spx/EXCLUDE`

**Demonstrable value:** `spx.status.json` carries an **authored** lifecycle status (`declared`/`specified`) — a hand-written "intentionally not passing yet" marker distinct from the machine-folded per-reference evidence outcomes. A node bearing it is **not discovered or run**, **not recorded as failing**, and **honored by CI** (passes the gate without its tests running). This lets authors write spec + test files without an implementation, keeps CI green, wastes no time discovering tests whose implementation does not exist, and **removes `spx/EXCLUDE`** — the per-node claim scopes the gate in its own committed record.

Detail (owned by `spx/31-spec-domain.enabler/21-node-status.enabler/PLAN.md`, summarized here):

- **Status-file contract** (`spx/31-spec-domain.enabler/21-node-status.enabler/15-status-file-contract.pdr.md`): add an authored lifecycle field beside the machine-folded evidence outcomes. Two concerns — the author owns the lifecycle intent; `--update` owns the evidence.
- **Fold:** `--update` folds per-reference evidence but **never overwrites the authored lifecycle status**; a `specified` node stays `specified` when evidence is absent or failing, never downgraded to `failing`.
- **Verification run / discovery:** skip nodes authored `declared`/`specified` (no wasted discovery, no failing record for absent implementations).
- **CI:** reproduces/refutes only nodes whose authored status expects passing; `declared`/`specified` nodes are not run and pass the gate.
- **Remove `spx/EXCLUDE`**; update the `CLAUDE.md` no-hand-edit rule so the lifecycle field is authorable while `passed`/`failed`/`not-run` evidence values stay machine-only.
- Governed by `15-status-file-contract.pdr.md` + `21-node-status-architecture.adr.md`. Reconcile with node-status's own evacuation plan (that node is slated to shrink; the authored-status contract may land in its future backend/spec-tree home).

### Later slices (deferred, defined)

- **Live observability.** For runs that rival agentic verification in length (~10 min), stream progress so agents and operators can inspect mid-run. Vitest supplies `onTestCaseResult` per-case events; a custom incremental reporter or tailable stdout capture into the scope. Generation streams (journal-shaped) while the durable artifact stays a snapshot.
- **Spec→test→source graph.** Materialize `spx/25-outcomeeng.enabler/31-spec-tree.enabler/21-graph.enabler` to replace the import-closure discovery/changed-set and to provide source-ownership staleness (source graph). This is the correct discovery/addressing spine the earlier slices reuse the relic for.
- **Retire the relic path.** Once the snapshot→fold→claim path is authoritative, retire the coarse `TestRunState` per-group recording (`spx/41-test.enabler/43-last-run-evidence.enabler`) and the `--update`-executes-tests behavior.
- **Python `test` verification type.** Extend the pytest adapter to the same snapshot recording (architecture is language-neutral through the registry; only TypeScript is built in slice 1).

## Prior coordination: verify command lifecycle

> Preserved from the earlier verify-command plan; still applies to the `spx/34-verification.enabler/32-verify.enabler` lifecycle cleanup, independent of the program above.

### Harness vocabulary alignment

Before applying this plan, read `spx/12-agent-harness.pdr.md` and use its vocabulary as the authority: agent harness, agent, agent adapter, and agent session. Treat nearby `agent`, `runtime`, `session`, `Claude`, or `Codex` wording as lower-layer/local vocabulary until reconciled; every touched spec, command text, source name, test, and pickup prompt names the precise harness role it describes.

`spx/12-agent-harness.pdr.md` includes top-level domains that journal verification runs executed by coding agents. Align this node's specs, verify lifecycle vocabulary, and verification-run vocabulary so the run journal is described as evidence for verification runs executed by agents, distinct from agent sessions, agent adapters, and SPX handoff session files. Keep CLI command names, help text, rendering, and bounded-output rules out of this library node; the `spx verification run` and `spx journal` command surfaces live under `spx/60-surfaces.enabler/21-cli-surface.enabler`.

1. Apply the parent `spx/34-verification.enabler/32-verify.enabler` cross-lifecycle assertions: lock the full operation mapping and the journal-event boundary, extend the existing-run scope-type and changeset-scope validation uniformly to the `input`, evidence-scope-add, and evidence-finding-add operations (the finish, status, and render operations already validate through `resolveExistingRun`), add the `spx verification run` descriptor's L2 CLI test for stdin and Commander wiring, then remove the parent entry from `spx/EXCLUDE` as its implementation begins passing.
2. Continue under `spx/34-verification.enabler/32-verify.enabler/PLAN.md`. That child plan owns run-set orchestration for repeated runs with expanding scope, and same-index type-specific nodes for `review` and `audit`.
3. Keep prompt wording changes separate from the CLI-interface slice; the command contract is the durable interface that prompt cleanup consumes.
