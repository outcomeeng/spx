# Bare Project Fixture

No language marker files. No `tsconfig.json`, no `pyproject.toml`, no ESLint config. Used to verify that spx validation tools skip cleanly when the target project has no detectable language toolchain.
